# Optional: Cognito (same as project.local.properties for Java)
# Copy to .env and set values, or use project.local.properties — Node reads both.
COGNITO_USER_POOL_ID=
COGNITO_REGION=us-east-1
COGNITO_CLIENT_ID=
COGNITO_DOMAIN=
PORT=8080
