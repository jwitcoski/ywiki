{
  "name": "ywiki",
  "version": "0.1.0",
  "description": "Simple Markdown wiki — Node server (Express), static frontend, optional Cognito",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "create-tables": "node scripts/create-dynamodb-tables.js",
    "ingest-parquet": "node scripts/ingest-parquet-to-wiki.js"
  },
  "license": "MIT",
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "jwks-rsa": "^3.1.0",
    "showdown": "^2.1.0"
  },
  "devDependencies": {
    "parquetjs": "^0.11.2"
  }
}
