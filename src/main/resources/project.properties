version=${project.version}
artifactId=${project.artifactId}

# Cognito (override with env: COGNITO_USER_POOL_ID, COGNITO_REGION, COGNITO_CLIENT_ID, COGNITO_DOMAIN)
# Set these or the env vars above so /auth/config returns configured=true and Sign in works.
cognito.userPoolId=
cognito.region=us-east-1
cognito.clientId=
cognito.domain=